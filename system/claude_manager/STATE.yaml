# =============================================================================
# STATE.yaml — Where We Are Right Now
# =============================================================================
# Updated by PM at every ::close
# Read by PM at every ::start
# =============================================================================

current:
  task: "File split (T4)"
  status: "COMPLETE"

resume_point: |
  Monolithic index.html (10,990 lines) split into 17 modular files + HTML shell (385 lines).
  Deployed to alfredramireza-tech.github.io/DND_Tool/ and verified working.

  New file structure:
    styles.css          — All CSS (1,029 lines)
    data/classes.js     — AASIMAR_SUBRACES, CLASS_DATA, COLOR_THEMES (180 lines)
    data/spells.js      — CANTRIP_DATA, SPELL_DB, spell enhancements, derived arrays (1,333 lines)
    data/progression.js — SKILLS, SAMPLE_CHARACTER, BLANK_DEFAULTS, CLERIC_PROGRESSION (308 lines)
    data/fighter.js     — FIGHTING_STYLES, MANEUVERS, FIGHTER_PROGRESSION, EK data (323 lines)
    js/utils.js         — mod(), AC calc, themes, character storage (461 lines)
    js/onboarding.js    — init(), form building, step navigation (1,144 lines)
    js/dashboard.js     — renderDashboard(), fighter dashboard (663 lines)
    js/levelup.js       — Full level-up wizard (1,352 lines)
    js/session.js       — Event log, roll history, dice animation (205 lines)
    js/combat.js        — Death saves, concentration, conditions, buffs, party view (1,390 lines)
    js/rolls.js         — Dice engine, weapon/ability/skill rolls (383 lines)
    js/equipment.js     — Selection cards, spell cards, dashboard rollers (333 lines)
    js/pdf-export.js    — PDF character sheet generation (517 lines)
    js/home.js          — Home screen, character management (233 lines)
    js/resources.js     — HP, spell slots, rest system, equipment forms (573 lines)
    js/cloud-sync.js    — GitHub API save/load (206 lines)
    index.html          — HTML shell + script tags + pdf-lib + boot (385 lines)

  PM can now load individual files instead of the full monolith.
  NEXT PRIORITY: Class implementations. Roll system needs flat bonus support and damage roll type before Barbarian can ship.

open_loops:
  - "Rogue/Barbarian/Monk at 25-30% — CLASS_DATA exists, no dashboard/level-up/subclass features. SPEC_BARBARIAN amended with roll system prerequisites."
  - "Paladin and Wizard at 0% — blocked on class implementation. SPEC_PALADIN amended with buff integration notes."
  - "Avatar images — designed but not spec'd (base64, 150x150, canvas resize, initials fallback)"
  - "PDF filler gaps — checkbox mapping, expertise handling, page 2 biographical fields"
  - "Roll system needs flat bonus support and damage roll type before Barbarian can ship (see SPEC_BARBARIAN amendment)"

recent:
  - "2026-03-26: File Split (T4) — 10,990-line index.html split into 17 modular files, index.html reduced to 385 lines"
  - "2026-03-26: Max HP Boost — getEffectiveMaxHp() helper, 24 hp.max refs audited, boost display, long rest clears"
  - "2026-03-26: Spec Amendments — SPEC_BARBARIAN roll prerequisites, SPEC_PALADIN buff integration notes"
  - "2026-03-26: Concentration/Buff Separation — 'Are you a target?' flow, custom buff editor, roll integration"
  - "2026-03-26: Dashboard Bug Fixes — scroll fix, condition rendering, modal overflow, Drop button fix, popovers"

gap_log:
  - date: "2026-03-26"
    gap: "renderConcentrationBanner() orphaned during HUD redesign — never called from dashboard"
    cause: "Combat HUD handoff replaced the HP tracker block that contained the banner call, and the new HUD only added a small concentration badge without Drop/Change controls"
    resolution: "Re-added renderConcentrationBanner(c) call below the sticky HUD"
    severity: "high — core feature (dropping concentration) was completely inaccessible"
  - date: "2026-03-26"
    gap: "Concentration auto-applied buff effects to caster regardless of spell target"
    cause: "Buff system and concentration were built in the same handoff and merged — no separation between 'I hold this spell' and 'this effect is on me'"
    resolution: "Full architectural separation — concentration tracks spell maintenance, externalBuffs tracks all stat effects, 'Are you a target?' prompt bridges them"
    severity: "high — caused incorrect AC calculations when casting buffs on teammates"
  - date: "2026-03-26"
    gap: "Max-height cap in sticky HUD spec caused visible slot clipping"
    cause: "Spec'd max-height: 160px looked reasonable on paper but clipped real content when resource strip wrapped to 2 lines"
    resolution: "Removed max-height and overflow: hidden. Compact HUD design keeps it short naturally."
    severity: "medium — resource strip was visually broken until caught"
  - date: "2026-03-26"
    gap: "PM could not load full index.html — context window exceeded by 10,990-line monolith"
    cause: "All code in single file from project start. Each feature session added ~500-1500 lines with no split point"
    resolution: "T4 file split into 17 modules. PM can now load any individual file. Session ran in CC with CC acting as both PM and CC."
    severity: "high — PM was unable to fulfill navigator/translator role without loading the codebase"
